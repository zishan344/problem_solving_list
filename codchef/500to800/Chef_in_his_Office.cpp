#include <bits/stdc++.h>
using namespace std;

int main()
{
  int t;
  cin >> t;
  while (t--)
  {
    int x, y;
    cin >> x >> y;
    int totalHr = (x * 4) + y;
    cout << totalHr << endl;
  }

  return 0;
}
